import React from "react";
import { createUseStyles } from "react-jss";

export const stylesPanelDescription = createUseStyles({
  title: {
    fontFamily: "BrawlStarsExtraBlack",
  },
  img: {
    maxWidth: "128px",
    maxHeight: "128px",
  },
});
